# Pentroid - Mobile Application Security Platform
#
# Every entry below is actually imported somewhere in app/ (verified via grep,
# August 2026 cleanup) -- no vestigial/unused packages from early scaffolding.

# Foundation
SQLAlchemy>=2.0.25
pydantic>=2.5.0
pydantic-settings>=2.1.0
PyYAML>=6.0.1
requests>=2.31.0          # dependency_manager.py (tool downloads) + virustotal_lookup plugin
cryptography>=42.0.0      # parses X.509 certs extracted from APK Signing Block v2/v3 (certificate_analysis)

# GUI
PySide6>=6.6.0
qtawesome>=1.3.0          # Material Design Icons, used by app/gui/icon_provider.py

# Reports
Jinja2>=3.1.3             # HTML report templates
reportlab>=4.0.9          # PDF report generation

# Malware / Dynamic analysis plugins (direct library deps, not Dependency-Manager tools)
yara-python>=4.5.0        # yara_scan plugin
frida>=17.0.0             # frida_instrumentation plugin (Python bindings, not the frida-tools CLI)

# Testing
pytest>=7.4.4

